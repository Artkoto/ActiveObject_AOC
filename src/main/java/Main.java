import Sched.Displayer;
import Sched.ObserverOfSensor;
import client.Canal;
import client.Sensor;
import client.SensorImpl;

import java.util.concurrent.*;

public class Main {

    public static  int TICK_NUMBER=1000;
    public static void main(String[] args) throws InterruptedException {

   /*   ScheduledExecutorService scheduler = new ScheduledThreadPoolExecutor(10);
        ObserverOfSensor observerOfSensor = new ;

        Sensor sensor = new SensorImpl();
        Canal canal1 = new Canal(scheduler);
        Canal canal2 = new Canal(scheduler);
        Canal canal3 = new Canal(scheduler);
      //  ObserverOfSensor Obs = new Displayer(scheduler, canal1, sensor);

       // Integer f = sensor.getValue();
     //   scheduler.scheduleAtFixedRate(()-> sensor.tick(),0, 500, TimeUnit.MILLISECONDS);
*/
    }
}
