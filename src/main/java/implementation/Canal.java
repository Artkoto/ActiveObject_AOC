package implementation;

import api.Capteur;
import api.Future;
import api.ObserverDeCapteurAsync;

public class Canal implements ObserverDeCapteurAsync {
    // todo ajout d'attribut

    @Override
    public Future update(Capteur s) {
        return null;
    }
}
